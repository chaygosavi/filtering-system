import { Product } from "@/db";

const Product = ({ product }: { product: Product }) => {
  return (
    <div className="group relative">
      <div className="aspect-h-1"></div>
    </div>
  );
};

export default Product;
